# Root-level environment reference
# Copy backend/.env.example to backend/.env and configure values before running.

# Ports (used by docker-compose.yml)
# FRONTEND_PORT=3100  -> mapped to container :3000
# BACKEND_PORT=3101   -> mapped to container :3001
